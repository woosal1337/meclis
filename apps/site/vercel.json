{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && bun install --frozen-lockfile && bun run build:site",
  "installCommand": "echo skipping default install",
  "outputDirectory": ".next"
}
